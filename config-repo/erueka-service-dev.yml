server:
  port: 10086
eureka:
  client:
    service-url:
      defaultZone: http://localhost:${server.port}/erueka
    register-with-eureka: false  #不对自己进行注册
    fetch-registry: false
  instance:
    hostname: localhost